{
  "name": "erg_testautomation",
  "version": "1.0.0",
  "description": "ERG UI Automation",
  "main": "index.js",
  "scripts": {
    "debug": "cross-env ENV=test PWDEBUG=1 DEBUG=pw:api cucumber-js --config=cucumber.js",
    "pretest": "npx ts-node src/support/report/init.ts",
    "test": "cross-env ENV=test FORCE_COLOR=0 cucumber-js --config=cucumber.js",
    "test:headless": "cross-env ENV=test FORCE_COLOR=0 HEAD=false cucumber-js --config=cucumber.js",
    "test:smoke": "cross-env ENV=test FORCE_COLOR=0 TAGS=@smoke cucumber-js --config=cucumber.js",
    "test:tag": "cross-env ENV=test FORCE_COLOR=0 TAGS=%npm_config_TAGS% cucumber-js --config=cucumber.js",
    "test:firefox": "cross-env ENV=test FORCE_COLOR=0 BROWSER=firefox cucumber-js --config=cucumber.js",
    "test:safari": "cross-env ENV=test FORCE_COLOR=0 BROWSER=webkit cucumber-js --config=cucumber.js",
    "test:browser": "cross-env ENV=test FORCE_COLOR=0 BROWSER=%npm_config_BROWSER% cucumber-js --config=cucumber.js",
    "posttest": "npx ts-node src/support/report/report.ts",
    "test:failed": "cucumber-js -p rerun @rerun.txt"
  },
  "dependencies": {
    "@types/node": "^20.14.12",
    "dotenv": "^16.4.5",
    "fs-extra": "^11.2.0",
    "multiple-cucumber-html-reporter": "^3.7.0",
    "playwright": "^1.45.3",
    "ts-node": "^10.9.2"
  },
  "devDependencies": {
    "@cucumber/cucumber": "^10.8.0",
    "@playwright/test": "^1.45.3",
    "@zerostep/playwright": "^0.1.5",
    "allure-commandline": "^2.30.0",
    "allure-cucumberjs": "^3.0.0-beta.9",
    "cross-env": "^7.0.3",
    "typescript": "^5.5.4"
  }
}
